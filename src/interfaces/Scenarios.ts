export interface Scenarios {
    symbol: string,
    title: string,
    potential: number,
    valuated: number,
    note: string,
    id: string,
    actual: number,
    expectedDate: string,
    createdAt: string,
    saveAt: string,
}